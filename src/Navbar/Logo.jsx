import React from "react";
import styled from "styled-components";

const LogoEl = styled.div`
  font-size: 1.5rem;
  font-weight: bold;
  color: white;
`;

const Logo = () => {
  return <LogoEl>Stripe</LogoEl>;
};

export default Logo;
    